#include "main.h"
/**
 * swap_integers - Swaps the values of two integer variables.
 *
 * This function takes two integer pointers as its arguments and swaps the
 * values of the integers they point to. After the function call, the values
 * of the two integers will be exchanged.
 *
 * @a: A pointer to the first integer.
 * @b: A pointer to the second integer.
 */
void reset_to_98(int *n)
{
*n = 98;
}
